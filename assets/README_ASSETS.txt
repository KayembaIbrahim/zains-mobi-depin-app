Place your logo image as ./assets/logo.png (PNG recommended).
Optional: place SimSun.ttf at ./assets/fonts/SimSun.ttf to enable SimSun typography on Android.
Then run: npx expo install expo-font && load the font if desired.
